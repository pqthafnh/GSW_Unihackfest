import { readGig } from "@/lib/gigs/service";
import { ClaimGigButton } from "@/components/gigs/gig-actions";
export default async function WorkerGig({ params }: { params: Promise<{ gigId: string }> }) { const { gigId } = await params; const { data: gig } = await readGig(gigId, "WORKER"); if (!gig) return <main className="p-12">Không tìm thấy công việc.</main>; return <main className="mx-auto max-w-3xl space-y-5 px-6 py-12"><h1 className="text-3xl font-semibold">{gig.title}</h1><p>{gig.description}</p><p>Trạng thái: <strong>{gig.status}</strong></p>{gig.status === "OPEN" && <ClaimGigButton gigId={gig.id} />}<p className="whitespace-pre-wrap rounded-xl bg-white p-5">{Array.isArray(gig.license_terms) ? gig.license_terms[0]?.terms_text : gig.license_terms?.terms_text}</p></main>; }
